# Controlled PiCC experiment

Implement the system described at {{SPEC_LOCATION}} within the fixed external
budget.

- {{CANDIDATE_GUIDANCE}}
- {{TEST_GUIDANCE}}
- {{REFERENCE_GUIDANCE}}

Work only in `/workspace`. Do not use the internet, inspect credentials or
harness internals, modify experiment-control files, or delegate to an existing C
compiler/interpreter. Implement general behavior rather than hard-coding tests.
Make reasonable decisions without asking for human intervention and spend the
session doing implementation work.
