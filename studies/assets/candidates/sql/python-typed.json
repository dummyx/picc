{
  "schema_version": 1,
  "language": "python",
  "framework": "strict-static-typing",
  "dependency_policy": "Python standard library only; no package installation",
  "build": {
    "command": [
      "mypy",
      "--strict",
      "--no-error-summary",
      "--cache-dir",
      "/tmp/mypy-cache",
      "pisql.py"
    ],
    "artifact": "pisql.py",
    "timeout_seconds": 300
  },
  "run": {
    "command": [
      "python3",
      "{artifact}",
      "{input}"
    ]
  },
  "source_extensions": [
    ".py"
  ],
  "audit": {
    "python_stdlib_only": true,
    "allowed_python_modules": [],
    "prohibited_patterns": [
      {
        "pattern": "\\brusqlite\\b|\\blibsqlite3\\b|\\bsqlite3_(?:open|exec|prepare)\\w*\\b|better-sqlite3|\\bDatabaseSync\\b|\\bduckdb\\b|\\blibsql\\b",
        "reason": "database engine library reference"
      },
      {
        "pattern": "#\\s*type:\\s*ignore\\b|#\\s*mypy:\\s*ignore-errors",
        "reason": "type-check suppression"
      }
    ],
    "prohibited_python_modules": [
      "sqlite3",
      "dbm",
      "shelve"
    ]
  },
  "spec_variables": {
    "LANGUAGE": "Python 3 with type annotations",
    "FRAMEWORK": "strict static typing (Python 3, `mypy --strict` must pass)",
    "DEPENDENCY_POLICY": "Python standard library only; do not install packages",
    "BUILD_COMMAND": "mypy --strict --no-error-summary --cache-dir /tmp/mypy-cache pisql.py",
    "ENTRY_COMMAND": "python3 pisql.py INPUT.sql",
    "PRODUCT": "PiSQL"
  },
  "agent_policy": {
    "blocked_bash_patterns": [
      {
        "pattern": "(^|[;&|]\\s*)(?:\\S*/)?sqlite3?(?=\\s|$)",
        "reason": "running an existing database engine is prohibited"
      },
      {
        "pattern": "\\bimport\\s+sqlite3\\b|\\bfrom\\s+sqlite3\\b|\\bnode:sqlite\\b|require\\(\\s*['\\\"]sqlite|\\bDatabaseSync\\b|\\brusqlite\\b|better-sqlite3",
        "reason": "delegating to an existing database engine is prohibited"
      }
    ]
  },
  "agent_notes": "The entry module is `pisql.py` in the workspace root; the fixed build command runs `mypy --strict` on it (following its imports) and fails on any type error, so a snapshot that does not type-check has no product. Write explicit type annotations for function signatures and data structures, prefer precise types over `Any`, and never suppress the checker with `# type: ignore` or `# mypy: ignore-errors`."
}
