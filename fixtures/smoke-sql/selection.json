{
  "schema_version": 1,
  "source": {"repository": "fixtures/smoke-sql", "revision": "local-smoke"},
  "scope": "smoke scripts for the SQL evaluator",
  "stages": [
    {"stage": 1, "family": "basic", "description": "setup, sort modes, hashing, labels, conditions", "scripts": ["test/basic/create_insert_select.test"]},
    {"stage": 2, "family": "rules", "description": "NULL semantics, join, halt", "scripts": ["test/rules/null_and_join.test"]}
  ]
}
